# CLAUDE.md

## ドキュメント管理

- 仕様書・設計書は必ず `documents/` フォルダに保存すること
- `documents/` 配下を編集する際は、必ず [documents/README.md](documents/README.md) のルールに従うこと

- 一つのタスクが完了して別のタスクに入る前にコミットを提案すること。

## Issue-driven 開発

Issue対応は `/issue {番号}` コマンドで開始する。
詳細な手順・フォーマット・ルールは `.claude/skills/issue/SKILL.md` を参照。